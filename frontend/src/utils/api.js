// API utility for handling base path
// Get base path from current location
export const getBasePath = () => {
  // Always use root path
  return '';
};

// API endpoint for sending emails
export const EMAIL_API_ENDPOINT = `/api/send-email`;

